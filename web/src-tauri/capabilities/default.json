{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability set for the Windows VisionData Gate workspace.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
